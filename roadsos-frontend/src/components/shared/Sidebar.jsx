import { Activity, BarChart3, Clock, HeartPulse, Home, Hospital, Map, Radio, Table2 } from 'lucide-react';
import { NavLink } from 'react-router-dom';
import { useSelector } from 'react-redux';
const links = { victim: [['/home', Home, 'Home'], ['/active', Radio, 'Active'], ['/first-aid', HeartPulse, 'First Aid'], ['/history', Clock, 'History']], responder: [['/responder', Radio, 'Responder'], ['/dashboard/map', Map, 'Map']], admin: [['/dashboard', Activity, 'Dashboard'], ['/dashboard/map', Map, 'Live Map'], ['/dashboard/incidents', Table2, 'Incidents'], ['/dashboard/resources', Hospital, 'Resources'], ['/dashboard/insights', BarChart3, 'Insights'], ['/dashboard/queues', Clock, 'Queues']] };
export default function Sidebar() { const role = useSelector((state) => state.auth.user?.role || 'victim'); return <aside className="hidden w-60 shrink-0 border-r border-border p-3 md:block">{(links[role] || links.victim).map(([href, Icon, label]) => <NavLink key={href} to={href} className={({ isActive }) => 'mb-1 flex items-center gap-3 rounded px-3 py-3 text-sm ' + (isActive ? 'bg-red-600 text-white' : 'text-slate-300 hover:bg-surface')}><Icon className="h-5 w-5" />{label}</NavLink>)}</aside>; }
