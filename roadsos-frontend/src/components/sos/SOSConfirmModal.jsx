import PropTypes from 'prop-types';
import { motion, AnimatePresence } from 'framer-motion';
export default function SOSConfirmModal({ open, onCancel, onConfirm }) { return <AnimatePresence>{open && <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} exit={{ opacity: 0 }} className="fixed inset-0 z-50 grid place-items-center bg-black/70 p-4"><motion.div initial={{ scale: 0.9 }} animate={{ scale: 1 }} exit={{ scale: 0.9 }} className="w-full max-w-sm rounded-lg border border-border bg-surface p-5"><h2 className="text-lg font-bold">Activate emergency SOS?</h2><p className="mt-2 text-sm text-slate-300">RoadSoS will share your GPS, alert responders, contact your emergency contacts, and start emergency guidance.</p><div className="mt-5 flex gap-3"><button onClick={onCancel} className="flex-1 rounded border border-border px-4 py-3">Cancel</button><button onClick={onConfirm} className="flex-1 rounded bg-red-600 px-4 py-3 font-bold text-white">Activate</button></div></motion.div></motion.div>}</AnimatePresence>; }
SOSConfirmModal.propTypes = { open: PropTypes.bool.isRequired, onCancel: PropTypes.func.isRequired, onConfirm: PropTypes.func.isRequired };
