import ResourceBadges from '../../components/hospital/ResourceBadges';
import { useListResourcesQuery } from '../../features/resources/resources.api';
export default function ResourceMonitor() { const { data, refetch } = useListResourcesQuery(undefined, { pollingInterval: 30000 }); const resources = data?.data || []; return <div><div className="mb-4 flex items-center justify-between"><h1 className="text-2xl font-bold">Hospital Resources</h1><button onClick={refetch} className="rounded bg-red-600 px-3 py-2">Refresh</button></div><div className="grid gap-3 md:grid-cols-2 xl:grid-cols-3">{resources.map((item) => <article key={item._id} className="rounded border border-border bg-surface p-4"><h2 className="font-bold">{item.hospital?.name}</h2><p className="text-sm text-slate-400">Last synced {new Date(item.lastSyncedAt).toLocaleString()}</p>{item.icuBeds < 3 && <p className="mt-2 rounded bg-red-700 p-2 text-sm">ICU below 10% threshold</p>}<div className="mt-3"><ResourceBadges resources={item} hospital={item.hospital} /></div></article>)}</div></div>; }
